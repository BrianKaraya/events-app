/* import React, { useContext } from 'react';
import { CartContext } from './CartContext';

const CheckoutCart = () => {
  const contextValue = useContext(CartContext);
  return (
    <div>
      <h5>Overview</h5>
      <div>
        <span>0</span>
        <br />
        <span>Total ammout: {contextValue}</span>
      </div>
    </div>
  );
};

export default CheckoutCart;
 */
